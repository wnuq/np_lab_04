name: Check Syntax

on:
  push:
    branches:
      - unittest

jobs:
  unit-testing:
    runs-on: ubuntu-latest 
    
    steps:
    - name : Checkout code
      uses : actions/checkout@v2
      
    - name : Run tests 
      run : python -m unittest test_app.py
